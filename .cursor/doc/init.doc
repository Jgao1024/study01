## 初始化项目

1. 帮忙初始化一个spring boot项目。
2. 统一所有接口返回值Result。
3. 接口注解校验，包含自定义的枚举扩展等可能用到的其他校验实现。
4. 自定义异常体系，通用的错误码，异常统一异常处理。
5. 帮忙链接MySQL，生成项目初始化数据库SQL，并配置好Mybatis
6. 建好包的层级关系，后续避免新建包，同时提供各层级包的Demo类便于后续参考，无需现在创建用户模块，Demo类保护
7. 整合knife4j用户文档管理
